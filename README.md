Syntax error that results in a runtime error.

Problem is an undefined variable is referenced, but the same variable name is used elsewhere nearby so the compiler does not seem to catch it.


## To reproduce

- Clone this repo
- `elm-reactor`
